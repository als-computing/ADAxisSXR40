# acquire/ — needs the camera

Single / Multiple / Continuous with the port-thread liveness probe after every stop,
software trigger, exposure actually applied (2 fps at 0.5 s), plugins wired and
delivering, pvAccess image type and dimensions, CA vs PVA pixel equality, pool cap,
timestamps (and the known gap: the camera record's own timestamp PVs stay 0).
`test_delivery_under_load.py` is full-tier.
