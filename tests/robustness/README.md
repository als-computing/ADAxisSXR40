# robustness/ — full tier; needs the camera

20 start/stop cycles at 32 rows with the deadlock probe after each (the August incident
geometry), binning 2×2 / 4×4 / back, a 5 s exposure, writes during acquisition, and pool
memory stable across two identical runs.
